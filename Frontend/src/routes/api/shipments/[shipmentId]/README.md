# Shipment Operations API

To find detail, status, participant, milestone, and document operations visit the nested route files under this directory. Each route resolves the active workspace and enforces role permissions before accessing a shipment.
